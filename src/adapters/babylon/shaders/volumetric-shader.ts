/** @deprecated Prefer load-shaders.ts — kept as a stable import path for binders/tests. */
export {
  VOLUMETRIC_COMPOSE_FRAGMENT,
  VOLUMETRIC_FRAGMENT,
  VOLUMETRIC_SAMPLERS,
  VOLUMETRIC_UNIFORMS,
} from './load-shaders';
