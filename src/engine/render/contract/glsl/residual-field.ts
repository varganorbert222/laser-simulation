/** GLSL twin of physics/optics residual density. */
import { RESIDUAL_FIELD_GLSL } from '../../../../generated/shaders/residual_field';

export function residualFieldGlsl(): string {
  return RESIDUAL_FIELD_GLSL;
}
