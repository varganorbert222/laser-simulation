/** GLSL twin of incidentLightDirection (srLightDir). */
import { INCIDENT_LIGHT_GLSL } from '../../../../generated/shaders/incident_light';

export function incidentLightDirGlsl(): string {
  return INCIDENT_LIGHT_GLSL;
}
