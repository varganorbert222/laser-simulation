/** @deprecated Import from `./quality` — kept as re-export for older paths. */
export {
  configureQualityRenderScale,
  getQualityRenderScaleConfig,
  renderScaleForPreset,
  type QualityRenderScaleConfig,
} from './quality';
